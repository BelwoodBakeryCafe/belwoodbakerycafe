import styled from 'styled-components';

import NormalVideo from 'components/Video';

const Video = styled(NormalVideo)`
  width: 100%;
  overflow-y: hidden;
  margin: 0 auto;
  display: block;
  top: 0;
  left: 0;
`;

export default Video;
